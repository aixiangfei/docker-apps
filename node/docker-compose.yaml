version: '3.8'

networks:
  backend:
    external: true

services:
  node:
    image: node:lts
    volumes:
      - /work/projects/:/work/projects/
    ports:
      - 8080:8080
    networks:
      - backend
    tty: true